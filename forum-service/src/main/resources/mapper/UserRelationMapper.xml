<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.forum.service.user.mapper.UserRelationMapper">

    <insert id="insert" parameterType="com.example.forum.service.user.entity.UserRelationDO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_relation (user_id, follow_user_id, follow_state, create_time, update_time)
        VALUES (#{userId}, #{followUserId}, #{followState}, NOW(), NOW())
    </insert>

    <update id="update" parameterType="com.example.forum.service.user.entity.UserRelationDO">
        UPDATE user_relation
        SET follow_state = #{followState},
            update_time  = NOW()
        WHERE id = #{id}
    </update>

    <select id="findByUserIdAndFollowUserId" resultType="com.example.forum.service.user.entity.UserRelationDO">
        SELECT * FROM user_relation
        WHERE user_id = #{userId}
          AND follow_user_id = #{followUserId}
        LIMIT 1
    </select>

    <select id="findFollowList" resultType="com.example.forum.service.user.entity.UserRelationDO">
        SELECT * FROM user_relation
        WHERE user_id = #{userId}
          AND follow_state = 1
        ORDER BY create_time DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="findFansList" resultType="com.example.forum.service.user.entity.UserRelationDO">
        SELECT * FROM user_relation
        WHERE follow_user_id = #{userId}
          AND follow_state = 1
        ORDER BY create_time DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countFollows" resultType="int">
        SELECT COUNT(*) FROM user_relation
        WHERE user_id = #{userId}
          AND follow_state = 1
    </select>

    <select id="countFans" resultType="int">
        SELECT COUNT(*) FROM user_relation
        WHERE follow_user_id = #{userId}
          AND follow_state = 1
    </select>

</mapper>
