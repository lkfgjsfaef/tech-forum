package com.example.forum.api.model.util.cdn;

public class CdnUtil {
    public static String autoTransCdn(String url) {
        if (url == null || url.isEmpty()) {
            return url;
        }
        return url;
    }
}
